{
  "name": "mubaddil",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "vercel-build": "npm install --prefix web && npm run build --prefix web",
    "test": "node --test tests/paymob.test.js tests/paymob-handlers.test.js"
  }
}
